server {
    listen 8080;
    listen [::]:8080;

    server_name _;

    root /var/www/website;
    index index.html;

    access_log /dev/stdout;
    error_log /dev/stderr warn;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ /\. {
        deny all;
    }
}
